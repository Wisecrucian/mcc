# mcc

Tooling around the internal `mcc` CLI.

- **[MCCPortForwarder](MCCPortForwarder/)** — the actual project here. macOS menu bar app for managing `mcc tp-port-forward` connections. See its own README for setup and usage, and CHANGELOG.md for what's changed.
- **mcc-intellij-plugin/** — dead. Only some leftover Gradle cache files got committed here, not the actual plugin source. Ignore it for now.
- **[IDEAS.md](IDEAS.md)** — random notes/ideas that don't fit anywhere else yet.
